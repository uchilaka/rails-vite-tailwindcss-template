# Gemfile
source 'https://rubygems.org'
# ruby '3.1.0'
gem 'rails'
# Needs to be right up here! Especially before any gems that need ENV variables
gem 'dotenv', require: 'dotenv/load'

gem 'rubocop'
gem 'erb_lint'
